<h1>Rooms</h1>

<ul>
  <%- @rooms.each do |room| %>  
    <li><%= room %> <%= link_to_edit edit_room_path(room) %> <%= link_to_delete room_path(room) %>  </li>
  <% end %>
</ul>

<%= link_to 'New Room', new_room_path, class: :button %>  
