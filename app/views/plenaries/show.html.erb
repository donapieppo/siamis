<%= render @conference_session %>  

<%- if user_in_organizer_commettee? %>  
  <%= link_to_edit2 'Edit', [:edit, @conference_session] %>  
  <%= link_to icon(:cog) + ' Add Chair', new_plenary_organizer_path(@conference_session), class: :button %>  
<% end %>

