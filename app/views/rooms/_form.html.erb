<%= simple_form_for @room do |room| %>  
  <%= room.error_notification %>
  <%= room.input :name %> 
  <%= room.input :capacity %> 
  <%= room.association :building %> 
  <%= room.input :floor, collection: (0..2), include_blank: false %> 
  <%= room.submit %>  
<% end %>
