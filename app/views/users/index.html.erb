<%= program_title('Users', 'users') %>  

<p class="alert alert-info">Users with (*) are registered.</p>

<p class="centered">
  <%= link_to 'New User', new_user_path, class: :button %>   
</p>

<ul class="list-group">
  <% @users.each do |user| %>  
    <li class="list-group-item">
      <strong><%= link_to user.cn, user, remote: true %></strong>
      <%= '(*)' if user.conference_registration %>  
      <span class="pull-right">
        <em><small><%= user.affiliation %></small></em> 
      </span>
    </li>
  <% end %>
</ul>

